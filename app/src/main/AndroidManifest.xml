<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="given.Consultants.moveit">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Driver_Services_dashboard"></activity>
        <activity android:name=".Services_user_dashboard" />
        <activity android:name=".User_Dashboard" />
        <activity android:name=".List_of_property" />
        <activity android:name=".Property_dashboard" />
        <activity android:name=".Driver_dashboard" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps_user"
            android:label="@string/title_activity_maps_user" />
        <activity
            android:name=".Maps_fr"
            android:label="@string/title_activity_maps_fr" />
        <activity android:name=".Property_create_acc" />
        <activity android:name=".Property_sign_in" />
        <activity android:name=".PropertyViewing" />
        <activity android:name=".Front_view_image" />
        <activity android:name=".Uploading_main_picture" />
        <activity android:name=".SliderView_Image_Firebase" />
        <activity android:name=".Driver_Create_Acco" />
        <activity android:name=".Driver_sign_in" />
        <activity android:name=".Property_main" />
        <activity android:name=".Create_Account" />
        <activity android:name=".Sign_in" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>